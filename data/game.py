class Game:
    def __init()__:
        self.running = True

    def main():
        while not self.done:
            lag += game.clock.tick(self.fps)
            self.event_loop()
            self.draw(lag/TIME_PER_UPDATE)